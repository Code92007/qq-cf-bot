services:
  qq-cf-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${BOT_PUBLISH_HOST:-0.0.0.0}:${BOT_PORT:-8088}:${BOT_PORT:-8088}"
    volumes:
      - ./data:/app/data
